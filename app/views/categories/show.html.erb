<p id="notice"><%= notice %></p>

<table>
	<tr>
		<td><h1><%= @category.name %><p>  </p></h1></td>

	  <td><%= link_to "Delete", @category, method: :delete, data: { confirm: "Really?" }%></td>

	  <td>
	</tr>
</table>
<%= link_to 'Edit', edit_category_path(@category) %> 
<%= link_to'Back', categories_path %>
